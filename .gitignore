# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
venv/
.venv/
env/

# Environment / secrets — NEVER commit the real .env
.env

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Logs (the file stays, but large log files shouldn't block pushes)
# fraud_alerts.log is intentionally tracked (small, useful for demo)

# Model checkpoints (large binaries tracked via Git LFS in .gitattributes)
# *.pkl is intentionally kept — tracked by LFS if needed

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/
